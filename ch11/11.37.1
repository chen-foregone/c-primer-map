#include<unordered_map>
#include<string>
#include<iostream>
using std::string;
int main()
{
	std::unordered_map<string, int>count;
	string word;
	while (std::cin >> word)
		count[word]++;
	for (auto t : count)
		std::cout << t.first << " " << t.second << "\n";
}